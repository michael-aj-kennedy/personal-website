import { Observable } from 'rxjs';
import { Input } from '@angular/core';

export class Education {
  id: number;
  yr: number;
  year: number;
  name: string;
  details: string;
  educationType: string;
}
